
    <view>
        <checkbox-group bindchange="dispatchEvent" data-change-uid="e38_32" data-beacon-uid="default">
            <block wx:for="{{state.items}}" wx:for-item="item" wx:for-index="i1130" wx:key="*this"><label>
                    <checkbox value="{{item.name}}" checked="{{item.checked}}" text="{{item.value}}" isRight="{{true}}" /></label></block>
        </checkbox-group>
    </view>
