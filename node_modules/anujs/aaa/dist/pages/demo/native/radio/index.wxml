
    <view>
        <radio-group class="radio-group" bindchange="dispatchEvent" data-change-uid="e25_49" data-beacon-uid="default">
            <block wx:for="{{state.items}}" wx:for-item="item" wx:for-index="i740" wx:key="*this"><label class="radio">
                    <radio value="{{item.name}}" checked="{{item.checked}}" text="{{item.value}}" isRight="{{true}}" /></label></block>
        </radio-group>
    </view>
