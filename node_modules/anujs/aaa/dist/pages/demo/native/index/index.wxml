
    <view class="anu-block">
        <view class="anu-page-header">{{state.title}}</view>
        <block wx:for="{{state.array}}" wx:for-item="item" wx:for-index="i1306" wx:key="*this">
            <view class='anu-item'>
                <navigator bindtap="dispatchEvent" open-type="navigate" hover-class="navigator-hover" url="{{item.url}}" data-tap-uid="{{'e50_32_' + i1306}}" data-beacon-uid="default">{{item.name}}</navigator>
            </view>
        </block>
    </view>
